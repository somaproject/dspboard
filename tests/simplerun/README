Simplerun:
  * Provides a 50 MHz clock to the DSPs 
  * Brings first DSP out of reset after 1 sec
  * Flashes the power LED and event leds in a pretty pattern
  * Let's DSPA control dspB-D reset



We also try and test if we have some sort of dynamic memory allocation
and static constructor running

