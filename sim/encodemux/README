The encode muxer encodes events and takes encoded data packets, and
muxes them out off the DSPBoard onto the DeviceLink. 

Here we simulate sending several events and data, and
verify that we recover the correct 8b/10b data streams. 

